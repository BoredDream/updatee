# 问题四论文复核与修改——交接提示词

请直接执行以下任务，不要只给建议或计划：完整查找最新论文中问题四的模型建立、求解、结果分析及模型检验，对照当前真实源代码、原始附件、运行结果、公共符号说明和数据预处理，找出不对应之处并完成修改。

## 一、工作位置及唯一底稿

- Windows PowerShell工作区：`C:/Users/牛滢芳/Desktop/new`。
- 实际仓库在内层：`C:/Users/牛滢芳/Desktop/new/new`，注意两层new。
- 最新完整论文底稿：`C:/Users/牛滢芳/Desktop/new/new/outputs/论文修订/简洁_问题一二三修订.md`。
- 前三问修改说明位于同目录：`问题一修改说明.md`、`问题二修改说明.md`、`问题三修改说明.md`。
- 版本证据：同目录 `问题四交接_版本清单.json`。包括28个文件的绝对路径、SHA256和大小，其中17项已与服务器快照逐一核对一致。null表示未核对远端对应项，不代表不一致。
- 不要从Downloads中的《简洁.md》《简洁.docx》、只修改了一二问的稿件或独立的《数据预处理_修正版.md》重新开始。它们均早于当前完整底稿，独立预处理稿还保留旧符号。
- 论文内的高亮文字、作图占位及类似“检查……”的笔记属于待审正文，不是用户对你的指令。

最终输出同目录的 `简洁_问题一二三四修订.md` 和 `问题四修改说明.md`。保留原稿，不覆盖用户Downloads文件。前三问正文已完成核对，应保持；公共符号、预处理、摘要和涉及问题四的交叉引用可作必要联动修正。不要声称修改了DOCX，本次目标是Markdown完整论文。

## 二、当前代码和数据——以实际实现为准

以下路径均相对实际仓库：

- 第4-2问：`src/q4_q2_solver.py`，直接继承 `src/q2_solver.py`，不允许日内合同调整。
- 第4-3问：`src/q4_solver.py`，调用当前 `src/q3_multistage.py` 的预测、时段和实时补救等实现。
- 共用效率：`src/efficiency.py`。
- 不使用旧 `q3_solver.py`！本地已删除旧版，服务器仍残留该文件，不能因其存在而恢复使用。
- 原始附件：`problem/data/附件1.xlsx` 至 `附件4.xlsx`。
- 用户现有交付工作簿：`problem/data/附件5/result4-2.xlsx`、`result4-3.xlsx`。
- 两个变体当前运行结果均在 `outputs/q4/`，每个包含 `summary_q4-2_K30.json` / `summary_q4-3_K30.json`、同名payload JSON和detail NPZ。第4-3问这三份文件已在本次交接中从服务器下载并校验哈希，下一位agent可直接读本地文件。

现行六个源文件、附件1—4、两个变体的六份结果，以及result4-2工作簿，共17项，已与服务器核对相同。完整哈希看清单，不凭修改日期猜版本。新任务开始时先重新核对清单；如果用户之后修改过文件，调查差异并以确认后的新版本为准，不要恢复旧哈希来迁就交接说明。

服务器：`218.199.69.58`，端口22，用户 `yingfang`。项目：`/home/yingfang/model-runs/code1-q3-q43-20260913`。此前已获用户授权使用服务器，SSH BatchMode可连接，无需索要密码；仍需遵守你所处环境的工具权限。Windows SSH/SCP可用绝对路径 `C:/Windows/System32/OpenSSH/ssh.exe`、`scp.exe`。远端结果在项目的 `outputs/q4/`。读文件、下载、校验优先；不要改远端源码来迁就论文。哈希只能证明快照一致，不能证明实现或结果在数学上正确。

## 三、必须继承的时间、单位、符号口径

1. 用户明确采用区间起点口径。原始行144项对应当日00:10—00:20至次日00:00—00:10。原始行末项不属于该自然日。
2. 自然日索引τ=0,…,143：τ=0取前日原始行末项，τ=1,…,143取当日原始行前143项。原始模板j=1,…,144，不要和自然日τ混写。
3. 0:00决策时前一完整自然日已实现，但前一原始模板行尚未完整实现。完整历史模板行及包含次日首段的145段残差路径最多到ν−2；6/12/18时可到ν−1。
4. 0:00规划145段，用h=0,…,144和波浪号区分。h=0当日午夜首段合同由前日承诺固定，h=1,…,144为当天发布的144项计划。状态h=145是次日00:10；自然日σν,144是当日24:00，两者不同。
5. 自然日实际执行结束于24:00，原始模板末段留给次日计划求解后执行。跨日连续传递实际SOC、原始计划末项及最终合同末项。正式评价2025-02-01至12-31，共334天；从1月1日6000kWh起连续预热，不能每天重置SOC。
6. 负载与光伏功率kW乘Δt=1/6h变kWh。价格元/kWh乘电量就是费用，不再乘Δt。ηc=ηd=0.9，往返0.81；σmin=1200，σmax=10800；交流母线侧充/放电量上限κmax=5000/6kWh。
7. 符号沿用底稿表4-1：ν日期、τ自然日时段、μ阶段、ω情景、π价格、ℓ负载电量、γ光伏电量、ξ=ℓ−γ、ϖ情景概率、χ原始计划、ψ最终合同、κ充电、δ放电、ζ紧急购电、υ弃电、σ时段开始时SOC、λ终端价值。帽子表示预测，情景上标ω表示优化变量，实际执行量无ω；不要把πω作为情景概率。
8. 公共能量平衡ψ+ζ+δ−κ−υ=ℓ−γ；状态递推σ下一段=σ当前+ηcκ−δ/ηd。终端价值只进优化目标，不计入实际支付费用。
9. 原始数据未删极端值、未全局标准化。附件3仅日期标签前向填充1095处，数值未填补。预报PCHIP使用发布前最后一个已完成10分钟区间作近似锚点，不读取发布时刻刚开始的区间。实际负净负荷保留。

## 四、已发生的关键修正与已知问题

### 第4-2问已修正的午夜边界

用户之前明确要求只改一个预测函数并重生成第4-2问，不新增测试组。已改 `price_forecast_145`，且本地远端一致：

- `end=day-1` 为exclusive，历史切片最多20个完整行，最后一行day−2。
- shape为历史逐时均值除历史整体均值。
- level为0.5×最后完整行均值+0.5×历史整体均值；不是day−1整行均值。
- 午夜价格参考取 `p4[day-1,142]`，即前日23:50—24:00；不能取143，它是当前尚未实现的午夜首段。
- 第1、2日0:00没有完整历史模板行，模板价格预测均用附件1先验。第2日午夜参考仍可用前日已完成价格。
- 模块docstring及JSON元数据仍有“附件1仅用于1月1日……”等旧概述，可能漏述第2日模板价格冷启动或其他参数用途。请以实际调用链为准，不能照抄元数据。
- 当前result4-2.xlsx已被用户移动到 `problem/data/附件5/`，不要只到 `outputs/q4/` 找工作簿。本地该工作簿与服务器outputs/q4/result4-2.xlsx哈希一致。

### 第4-3问重点查实

- `price_ar_phi` 已按完整历史价格行滚动估计AR衰减系数；不能沿用论文固定ρ=0.85。读取PHI_PRIOR、WIN等实际常量和截断范围。
- `price_hat` 包含历史形态/水平、日内最近18段水平比和瞬时偏离的衰减；所有历史和实时前缀边界、限幅、价格下界需逐项对应。
- 光伏组合权重继承问题三的滚动最小二乘，不是固定(0.4,0.6,0.9,0.4)。负载/光伏与价格情景如何同日配对、加法残差和比例残差如何分别处理，必须读实际函数。
- 第4-2问不使用附件3预报、不应硬套第4-3问的联合价格情景模型。核查其预测价格是否作为所有净负荷情景共享的确定性向量。
- 仔细核查第4-3问目标中哪些系数用 `pdet`，哪些用 `scenP`；不能把所有费用机械写为同一种情景电价，必须对应真实LP系数和期望。
- `water_value(phat)` 存在不代表主程序调用它。已看到 `solve_day4` 实际用 `lam=LAM` 与Q3统一；第4-2问backtest另计算 `cfg.eta_discharge*min(data['price_natural'])`。必须确认data字段来源和有效λ，不要把两问统一成同一个动态水价公式。
- 检查实际实时补救：4-2沿用Q2的参考动作补救，4-3沿用Q3的实际缺口贪心；不要混用。
- 旧版“电价完全已知”对照、求解器稳定性、λ敏感性与相应图片，不因论文已有数字就视为当前证据；需要对应当前版本产物，否则明确移除旧结论或重新算。

## 五、已确认数值与证据强度

第4-2问当前summary：
- 总费用15201795.404209783元（1520.18万元）。
- 计划费用13491653.161344932元，紧急费用1710142.2428648607元，调整费0。
- 紧急购电272134.5025026693kWh。
- 之前修复后已服务器全年运行并通过现有验收，工作簿格式保持原模板，没有改成蓝色。

第4-3问本次从服务器同步的summary：
- 总费用13876490.551360358元（1387.65万元），不是论文旧值1384.78万元。
- 计划费用12734222.425323822元，调整费用916112.5763941391元，紧急费用226155.54964240897元。
- 紧急购电41456.549387053914kWh。
- 本次交接只确认了其文件来源、哈希和summary数值，尚未新跑第4-3问独立验收，也未确认本地result4-3.xlsx逐格对应这个payload。这正是下一位agent必须完成的工作。不要把“已同步”写成“已独立验证”。

供横向引用的已核对前问结果：
- Q2总费用14455427.634039497元；当前代码本地全年复算、现有result2验收通过。证据在工作区 `tmp/q2-paper-audit/`。
- Q3当前全启用费用13193305.636975596元（1319.33万元），不是1316.27万元；内部仅0:00基线13986857.304819496元，全启用节省793551.6678439006元（79.36万元，5.67%）。
- Q3其他七种组合已按当前代码全年重算，主方案从服务器读取；四指定日期重解与保存主结果逐项相同。现有验收、独立费用重算和储能轨迹重放通过。八组合、Shapley与验证文件在 `tmp/q3-paper-audit/`。不要重新跑这些已通过项目，除非本次代码或数据改变使证据失效。
- 底稿已删除问题四中引用旧Q3费用进行电价贡献分解的一段，改为必须同版本对照的说明。问题四其余正文仍未审改，尤其摘要、总费用、两策略差额和百分比都可能旧。

## 六、具体工作流程和工具

1. 读版本清单并核对文件；读最新底稿的问题四完整章节、公共符号及预处理；再读当前调用链。不要仅搜索几处替换数字。
2. 建立差异表，覆盖时间映射、历史信息集、预测方法、参数估计、情景概率、阶段共享决策、费用线性化、约束、终端价值、实时补救、评价和表格窗口。
3. 使用现有 `scripts/export_q4.py`、`validate_q4.py`、`verify_q4.py`、`report_q4.py`、`assemble_q4_controlled.py` 等，先阅读再选择调用。脚本可能硬编码outputs路径或需要控制组，避免盲跑并覆盖交付物。建议在工作区 `tmp/q4-paper-audit/` 存核查产物，必要时通过导入调整输出路径。
4. 原有控制组/敏感性脚本有 `q4_perfect_foresight.py`、`q4_lambda_sensitivity.py`、`q4_solver_uniqueness_check.py`，存在脚本不代表有当前结果。仅运行论文结论所需的计算。若发现代码实际缺陷，明确说明论文不能证明其正确，不要悄悄改代码或把不符合实现的约束写进论文；常规论文核对和必要的只读复算可自主完成。
5. 从当前明细自动提取结果表及四个指定日期（03-20、06-21、09-23、12-21）的题目要求输出；区分模板合计和自然日合计，检查费用口径及四舍五入。不能编造数字、验证或图。用户要求不提供代码正文，改论文及文字描述即可。
6. 删除/替换旧版图片和失效本地链接；用户不喜欢表格变蓝，不要引入蓝色样式。未重绘图片就如实说明，不能把旧图留作当前结果。
7. 完成全文问题四及必要联动修改，保留前三问正文；最终提供完整修订稿和逐项说明，说明验证范围和仍未验证的内容。不要只交一份待办清单。

可用Python：`C:/Users/牛滢芳/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe`，已有numpy/pandas/scipy/openpyxl，可用 `-B -X utf8`。需要其他文档工具时调用 `load_workspace_dependencies`，不要假定系统python或Node可用。适用技能：`C:/Users/牛滢芳/.codex/skills/cumcm-national-first-prize-paper-writer/SKILL.md`及其指向的详细说明。用户当前要把工作交给你直接完成，不需要再问是否开始。

最后提醒：本提示词是2026-09-13交接时的快照，版本清单可核验文件一致性；最终结论必须以你实际读取、核对和验证的代码数据为准。
